fn call() {
    // Tuples
    let coord = (2, 3);
    let (x, y) = (2, 3);
    let (name, age) = ("Vino", 34);
    println!("{:?} {:?}", coord.0, coord.1);
    println!("{:?} {:?}", x, y);
    println!("{:?} {:?}", name, age);
}
